# Root env used by docker-compose for the Postgres service.
# Copy to .env and edit. The API's own secrets live in backend/.env
POSTGRES_USER=proapp
POSTGRES_PASSWORD=change_this_strong_password
POSTGRES_DB=proapp
